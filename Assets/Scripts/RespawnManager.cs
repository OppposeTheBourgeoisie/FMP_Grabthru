using UnityEngine;

public class RespawnManager : MonoBehaviour
{
    public Transform globalRespawnPoint;  // Set this in the inspector to a fixed respawn point in your scene

    // You could also handle the respawn logic here if needed
}
